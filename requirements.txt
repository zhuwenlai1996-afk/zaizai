# ===== 老照片修复工具依赖 =====
#
# 安装顺序很重要, 请直接使用:
#   pip install -r requirements.txt
#
# 如果在中国大陆, 建议先配置清华源:
#   pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
#
# CUDA 用户: 先按 https://pytorch.org 选择对应 CUDA 版本的 torch 安装命令,
# 不要直接 pip install torch (会装到 CPU 版).

# --- 深度学习框架 ---
# CPU 默认; GPU 用户请自行替换为 cu118 / cu121 等版本
torch>=2.0,<2.5
torchvision>=0.15,<0.20

# --- 图像 / 数值 ---
numpy>=1.23,<2.0
opencv-python>=4.7
Pillow>=9.5

# --- 修复模型 ---
# basicsr 1.4.2 是最后一个稳定版, 但与新版 torchvision 不兼容,
# 我们在 photo_restore/compat.py 中打了运行时补丁
basicsr==1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# basicsr 隐式依赖
tqdm>=4.65
pyyaml>=6.0
addict>=2.4
yapf>=0.40
future>=0.18
lmdb>=1.4
tb-nightly; python_version<"3.13"
scipy>=1.10

# --- 打包 (仅打包 exe 时需要) ---
# pip install pyinstaller>=6.3
